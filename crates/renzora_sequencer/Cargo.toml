[package]
name = "renzora_sequencer"
version = "0.1.0"
edition = "2021"

[dependencies]
renzora = { path = "../renzora", default-features = false, features = ["editor"] }
# bevy_ui-native panel content (the egui → bevy_ui migration).
renzora_ember = { path = "../renzora_ember" }
bevy = { workspace = true }
serde = { version = "1", features = ["derive"] }
ron = "0.8"

[lints]
workspace = true
