[package]
name = "test_plugin"
version = "0.1.0"
edition = "2021"
description = "A test plugin for the Bevy Editor"

[lib]
crate-type = ["cdylib"]

[dependencies]
editor_plugin_api = { path = "../editor_plugin_api" }
