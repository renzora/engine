[package]
name = "renzora_theme"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18" }
bevy_egui = "0.39"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
serde_json = "1.0"
