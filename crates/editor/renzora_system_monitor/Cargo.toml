[package]
name = "renzora_system_monitor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

[dependencies]
bevy = { workspace = true }
renzora = { path = "../../renzora", default-features = false, features = ["editor"] }
sysinfo = "0.35"
nvml-wrapper = "0.10"
