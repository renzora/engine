[package]
name = "system_monitor_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
editor_plugin_api = { path = "../editor_plugin_api" }
sysinfo = "0.32"
nvml-wrapper = "0.10"
