[package]
name = "renzora_environment_map"
version = "0.1.0"
edition = "2021"
description = "Image-based lighting (IBL) component — controls sky-derived environment map intensity on cameras. Independent of the atmosphere effect; can be added/removed via inspector."

[features]
default = []
editor = ["dep:renzora_editor", "dep:egui-phosphor", "dep:bevy_egui", "dep:renzora_theme"]

[dependencies]
bevy = { workspace = true }
serde = { version = "1", features = ["derive"] }
renzora = { path = "../renzora", default-features = false }
egui-phosphor = { version = "0.11", features = ["regular"], optional = true }
bevy_egui = { version = "0.39", optional = true }
renzora_editor = { path = "../renzora_editor", optional = true }
renzora_theme = { path = "../renzora_theme", optional = true }
