[package]
name = "renzora_shape_library"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

[features]
default = ["editor"]
editor = []

[dependencies]
bevy = { workspace = true }
renzora = { path = "../renzora", default-features = false, features = ["editor"] }
