[package]
name = "renzora_gauges"
version = "0.1.0"
edition = "2021"

# No `editor` feature: lean runtime crate. The Gauges inspector + native debug
# panel moved to the nested renzora_gauges_editor crate (crates/renzora_gauges/editor).

[dependencies]
bevy = { workspace = true }
bevy_gauge = "0.4"
renzora = { path = "../renzora", default-features = false }

[lints]
workspace = true
