//! Empty stand-in for the `lightyear_steam` registry crate — see Cargo.toml
//! for why. Never compiled in practice: lightyear's `steam` feature is not
//! enabled anywhere in the workspace. If you hit a compile error inside this
//! crate, something turned that feature on; remove the `[patch.crates-io]`
//! entry in the workspace root instead of adding code here.
