[package]
name = "dynamic_plugin_loader"
version = "0.1.0"
edition = "2021"
description = "Loads Bevy plugins from DLLs at startup. Full &mut App access."

[dependencies]
bevy = { workspace = true }
dynamic_plugin_meta = { path = "../dynamic_plugin_meta" }
libloading = "0.8"
