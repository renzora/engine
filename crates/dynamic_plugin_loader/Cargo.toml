[package]
name = "dynamic_plugin_loader"
version = "0.1.0"
edition = "2021"
description = "Loads Bevy plugins from DLLs at startup. Full &mut App access."

[dependencies]
bevy = { workspace = true }
renzora = { path = "../renzora", default-features = false }
libloading = "0.8"
