[package]
name = "renzora_hui"
version = "0.1.0"
edition = "2021"
description = "Transitional facade — the HUI markup runtime + icon/cursor helpers were folded into renzora_ember; this crate re-exports them so renzora_hui::* paths keep compiling until callers migrate."

[dependencies]
renzora_ember = { path = "../renzora_ember" }

[lints]
workspace = true
