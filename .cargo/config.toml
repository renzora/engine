# Faster linking for Windows (uses rust-lld)
[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"

# Faster linking for Linux (uses lld via clang)
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=lld"]
